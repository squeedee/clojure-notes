(keys (ns-publics 'clojure.java.io))
; -> output-stream copy file writer as-relative-path
;    make-output-stream make-parents reader
;    default-streams-impl delete-file as-file resource
;    input-stream Coercions make-input-stream
;    make-writer as-url make-reader IOFactory

;; ..but dir is still easier

(dir clojure.java.io)
